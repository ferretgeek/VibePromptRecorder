# Vibe Prompt Recorder 项目规则

- 当前实现位于 `开发文件夹/`，长期文档位于 `需求和方案文件夹/`；`成品文件夹/` 只包含本机成品与用户数据，永远不得提交或公开。
- 项目保持本地离线定位：不得擅自增加模型 API、账号系统、遥测、云同步或后台常驻服务。
- 公开示例、测试和截图只能使用明显的占位值、保留示例域名与合成提示词；不得出现真实姓名、账号、提示词、用户目录、数据库、备份或密钥。
- 修改后至少运行 `pnpm lint`、`pnpm typecheck`、`pnpm test` 和 `pnpm build:web`；涉及 Rust、数据、恢复或打包时运行 `pnpm verify`。
- 字体文件必须继续通过 `pnpm audit:fonts`；白名单、哈希和许可不一致时不得打包或发布。
- 数据库 schema、项目包与备份格式需要保持向后兼容；必须迁移时同步版本、恢复文档、需求追踪与测试。
