# 提示词记录工具最新版修复清单

- 核实日期：2026-07-31
- 目标版本：0.1.4（个人自用便携版）
- 唯一有效清单：本文件。后续只更新本文件，不保留带版本号的旧修复清单。
- 核实范围：`审查报告-Codex.md` 的 P1–P8、R1–R7；`安全与敏感项-仅报告.md` 的 VPR-SEC-001–008；当前源码、锁文件、测试、0.1.3 成品元数据。
- 基线验证：Vitest 21 个文件、131 项通过；Rust 72 项通过、1 项手动性能测试按设计忽略。

## 一、逐项核实结论

### `审查报告-Codex.md`：已确认问题

| 编号 | 核实结论 | 当前证据与处理决定 |
| --- | --- | --- |
| P1 | 确认 | `DetailPane` 从“安全预览”切回“所见即所得”时没有清除 `readOnlyPreview`，切换轮次也没有重置。修复并补回归测试。 |
| P2 | 确认 | 完成轮次在 `flushActive()` 前设置编辑锁；锁定期间输入被 `inert` 和 store 拒绝。改为保存期间继续接收输入，由保存循环追上最新编辑，真正切换数据库状态前才锁定；同时保留并发去重。 |
| P3 | 部分误报、契约缺口确认 | `fetch_remote_image` 的浏览器命令映射确实缺失，但当前 `MarkdownPreview` 在浏览器壳中会提前阻止调用，因此“点击必然抛未知命令”不成立。补齐显式浏览器适配器与契约测试，保持浏览器不直接联网。 |
| P4 | 确认 | `verify:rust` 和便携打包在干净目录中可能先运行依赖生成字体的 Rust 测试。把 `prepare:tauri` 固定放到 Rust 验证前，并增加顺序门禁。 |
| P5 | 确认 | WebView2 可再生缓存没有清理边界。启动时只清理名称和层级均在白名单内的缓存目录，保留 Preferences、Local Storage、Network 等持久状态。 |
| P6 | 确认（遗留现场 + 生命周期缺口） | 0.1.3 当前数据目录仍有未截断 WAL。正常关闭前执行 `wal_checkpoint(TRUNCATE)`，成功后才登记 clean-shutdown；不直接操作真实数据库正文。 |
| P7 | 确认 | `vpr:fonts-ready` 没有监听者。字体加载完成后让虚拟时间线按已保存锚点重新测量，避免字体切换后的卡片高度漂移。 |
| P8 | 观察成立，不作为缺陷改写架构 | FTS5 trigram 对大正文存在索引体积和全量重建成本，但这是中文子串搜索的明确权衡，当前 10 MiB 单轮上限和性能样本仍可接受。补充量化说明，不冒险更换 tokenizer 或延迟搜索一致性。 |

### `审查报告-Codex.md`：需进一步验证的风险

| 编号 | 核实结论 | 当前证据与处理决定 |
| --- | --- | --- |
| R1 | 风险确认 | 小于 256 KiB 的正文每次击键都会立即向 Worker 克隆全文。增加短防抖，大正文继续使用更长防抖，并补时序测试。 |
| R2 | 风险确认 | 超时后的 `mark_clean_shutdown` 与 `cancel_clean_shutdown` 可能乱序。改为 Rust 侧带单调 generation 的关闭意图协议，迟到的 mark 不得覆盖 cancel。 |
| R3 | 误报 | `state.db.lock().query_row(...).optional()` 的临时锁在该语句分号处已释放，`fs::read` 不在数据库锁内。无需修改读取路径，只补注释防止未来误改。 |
| R4 | 设计护栏，不是缺陷 | 1 秒 progress handler 使用独立只读连接，超时有明确可恢复提示；1000 轮样本远低于阈值。保留，避免无界搜索拖垮进程。 |
| R5 | 风险确认 | 仅枚举进程默认注册表视图可能漏掉 32 位字体项。Windows x64 同时读取 64 位与 32 位字体注册表视图并去重。 |
| R6 | 未发现缺陷 | 恢复状态机已经用 phase 和原子 rename 覆盖崩溃窗口，现有中断/回滚测试通过。保持实现。 |
| R7 | 当前无触发路径 | 设置只在设置页内写入，打开期间没有外部设置写入方。保持实现；若未来增加外部写入口再改为版本化草稿。 |

### `安全与敏感项-仅报告.md`

| 编号 | 核实结论 | 当前证据与处理决定 |
| --- | --- | --- |
| VPR-SEC-001 | 确认 | UNC 只做文本前缀判断，映射网络盘可绕过。Windows 上调用卷类型 API 拒绝 `DRIVE_REMOTE`，并补路径解析测试。 |
| VPR-SEC-002 | 确认 | 启动期字体对账传入 `verify_hash=false`。改为启动期校验 SHA-256；内容不匹配的字体在暴露给 WebView 前标记不可用。 |
| VPR-SEC-003 | 确认 | 备份恢复验证主要覆盖文件/结构/计数，没有完整业务语义。增加项目、轮次、草稿、排序、时间、revision、视图引用和 FTS 内容一致性验证及拒绝测试。 |
| VPR-SEC-004 | 确认 | 读取型命令接受 renderer 任意路径。把 Markdown、项目包、完整备份、字体的文件选择移动到 Rust 原生命令内，移除 renderer 的文件打开权限和未使用的 dialog 插件。 |
| VPR-SEC-005 | 确认 | AVIF 通过字节搜索 `ispe`，不能建立可靠画布边界。当前需求不强制 AVIF；移除 AVIF 远程加载支持并明确拒绝，保留 PNG/JPEG/GIF/WebP/BMP/ICO 的有界解析。 |
| VPR-SEC-006 | 确认 | 自动备份只看文件名前缀。仅把扩展名正确、可打开、manifest 校验通过且内部 `createdAt` 属于当天/当周的真实归档用于去重；损坏项不阻止新备份。 |
| VPR-SEC-007 | 事实成立、当前产品边界内非缺陷 | 当前明确是个人自用成品，没有受信代码签名证书，也不宣称公开分发。不会用自签名伪装受信发布；manifest 继续如实标记 unsigned。公开分发时必须另行提供受信证书和时间戳服务。 |
| VPR-SEC-008 | 确认 | `quick-xml 0.38.4`、`time 0.3.45` 命中已修复版本之前的 RustSec 通告，直接依赖 `ttf-parser` 已停止维护。定向更新 `plist/time` 传递链，迁移到维护中的 Fontations 字体解析器，使用最新 RustSec 数据复扫；同时修复 P4。 |

## 二、执行清单

- [x] F01 修复安全预览模式切换与轮次切换状态，补前端回归。
- [x] F02 重构完成轮次的保存/锁定顺序并保留并发去重，补慢保存期间继续输入的回归。
- [x] F03 补齐浏览器远程图片命令契约，验证浏览器壳绝不直连远程图片。
- [x] F04 修复干净目录下字体准备与 Rust 验证/打包顺序。
- [x] F05 增加 WebView2 可再生缓存白名单清理及保留未知文件测试。
- [x] F06 正常关闭前截断 WAL，并用 generation 消除关闭意图乱序。
- [x] F07 接入字体完成后的虚拟时间线重测；为 Markdown 安全分析增加分级防抖。
- [x] F08 拒绝映射网络盘作为活动数据根。
- [x] F09 启动期完整校验已导入字体哈希，并同时枚举 Windows 32/64 位字体注册表视图。
- [x] F10 增加备份数据库业务语义与 FTS 内容一致性验证。
- [x] F11 把四类本地文件读取选择移到 Rust，收紧 renderer capability 并删除未使用插件。
- [x] F12 移除不可靠的远程 AVIF 支持和相关 Accept 声明，补拒绝测试。
- [x] F13 修复自动备份去重，损坏/伪造文件名不得阻断备份。
- [x] F14 更新易受影响的 Rust 依赖，替换停止维护的字体解析器并执行最新安全复扫。
- [x] F15 更新 FTS 量化说明、需求追踪、README、CHANGELOG、发布说明与验收报告到 0.1.4。
- [x] F16 完整执行格式、lint、类型、Vitest、Web 构建、Rust fmt/clippy/test、Windows x64 Release、Playwright 三视口和便携包验证。
- [x] F17 生成并验证 0.1.4 个人便携成品；迁移现有 `data/` 后只保留 0.1.4 当前使用目录、ZIP、manifest、依赖清单和最新版文档，移除 0.1.3 程序/包/版本化发布文档，不删除用户数据。

## 三、最终验证与成品

- 前端：Prettier、ESLint、TypeScript、Web 构建通过；Vitest 21 个文件、135 项通过。
- Rust：Rustfmt、Clippy `-D warnings`、81 项测试通过；1 项手动性能入口按设计忽略。
- 浏览器：最终打包门禁 21 项通过、27 项按视口/场景设计跳过；桌面、平板、手机实看无溢出。
- 安全：`pnpm audit` 0 个已知漏洞；最新 RustSec 数据扫描 0 个 vulnerability。
- 成品：0.1.4 ZIP 目录与二次解压验证通过，SHA-256 为 `85fffba114b00ec8b2cd46431618e7c54015bc15f7a84ef98ffc32db14990892`。
- 数据：迁移前 206 个文件、12,383,441 字节逐文件校验一致；完整备份位于 `成品文件夹/用户数据备份/upgrade-to-0.1.4-20260731-205212/`。
- 原生：现有数据副本安全关闭通过；当前数据 `app.sqlite3-wal` 为 0 字节，clean-shutdown 标记存在。

## 四、完成标准

1. 所有确认问题都有代码修复或明确的当前产品边界，不把误报和观察项伪装成代码缺陷。
2. 所有新增安全边界都有自动测试；数据恢复、自动保存、关闭、字体和归档主路径无回归。
3. RustSec 最新复扫不再包含本项目 Windows x64 实际可达的已知 vulnerability；停止维护的直接字体解析依赖被移除。
4. 0.1.4 便携包可从目标入口启动并安全退出，现有数据完整保留，正常退出后的 WAL 已收敛。
5. 项目只保留 0.1.4 最新发布物和最新版说明；`CHANGELOG.md` 可保留历史变更文字，但不保留旧版独立成品、独立验收/发布说明或旧 manifest。
